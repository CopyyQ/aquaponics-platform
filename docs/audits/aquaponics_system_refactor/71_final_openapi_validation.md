# OpenAPI validation

The fresh source export has 47 paths and 82 operations. It has no `/admin`, `/projects`, or overview operations, exactly one MQTT export operation, no public notification operation, and no duplicate operation IDs. The prohibited retired device and feedback terms have zero occurrences.
