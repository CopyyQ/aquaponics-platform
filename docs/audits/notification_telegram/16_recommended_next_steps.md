# Recommended next steps

After the canonical threshold runtime is complete, route canonical threshold
incidents exclusively through outbox, render from incident snapshot only,
replace broad policy skip reasons, and update project notification history for
rule-less incidents. Keep direct recipient test explicitly separate from the
operational notification path.
