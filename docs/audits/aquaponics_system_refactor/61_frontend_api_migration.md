# Frontend API migration

Auth state hydrates effective permissions from `/auth/session`; role options include TECHNICIAN. Typecheck, Vitest and production build pass from the current frontend workspace.

