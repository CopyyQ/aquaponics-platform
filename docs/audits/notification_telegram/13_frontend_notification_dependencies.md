# Frontend dependencies

The project notifications page consumes notification settings and recipients.
It informs administrators that `TELEGRAM_BOT_TOKEN` is server-managed. No
frontend Bot token, chat-ID fallback or Telegram HTTP client was found.
