# Frontend legacy inventory

The active `frontend/src` and `frontend/e2e` trees contain no matches for the retired route prefixes or retired device-classification terms. The canonical client uses `/aquaponics-systems` as its runtime root.

Validation on 2026-09-08: TypeScript typecheck, Vitest, ESLint, and production build all passed.
