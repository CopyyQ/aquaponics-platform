# Legacy schema inventory

Revision 0035 still contains `device_templates.device_kind`, `sensors.purpose`, `actuator_feedback_bindings`, feedback definitions, legacy Sensor/DeviceTemplate/Actuator threshold columns, `alert_rules`, and `sensor_alerts`. These objects were retained during rehearsal because their rows require explicit mapping or historical retention; no unverified drop was performed.
