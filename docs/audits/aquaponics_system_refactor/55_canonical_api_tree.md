# Canonical API tree

The public router exposes `/auth`, `/users`, `/aquaponics-systems`, `/device-templates`, `/sensor-models`, and `/actuator-models`. Nested operational routes are system scoped, and a device detail returns both `sensors[]` and `actuators[]`.

