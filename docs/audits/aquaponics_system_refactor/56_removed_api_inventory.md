# Removed API inventory

Legacy `/admin`, `/projects`, generic overview, global device/sensor/telemetry and public notification routers are excluded from `api_router`; requests therefore resolve to 404. The removed prefixes are recorded in `removed_routes.json`.

