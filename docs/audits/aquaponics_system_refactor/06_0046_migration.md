# 0046 rehearsal migration

The rehearsal upgraded from 0045 to 0046 successfully. It removes type-enforcement triggers, re-merges deterministic split device pairs, archives legacy classifications, renames `projects` to `aquaponics_systems`, and renames each domain foreign key to `aquaponics_system_id`.
