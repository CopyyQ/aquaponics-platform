# Backend regression

On PostgreSQL rehearsal revision 0050, `python -m pytest -q` passed: 27 passed, 0 failed. `compileall` and the architecture-boundary checker also passed. Two FastAPI ORJSON deprecation warnings remain and do not fail the suite.
