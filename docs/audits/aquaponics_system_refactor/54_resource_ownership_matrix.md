# Resource ownership matrix

Global catalogs and users use explicit catalog permissions. Systems, devices, sensors, actuators, telemetry, thresholds, alerts, members, activities, SCADA and MQTT export require system scope through `require_project_access`.

